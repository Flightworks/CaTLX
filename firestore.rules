rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // K1 intentionally starts fail-closed. K2 adds the tested role-scoped rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
